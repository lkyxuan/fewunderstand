# Few Understand 社区版架构设计

## 项目定位

**信号系统 + 新闻热点**，面向普通用户的加密货币信息平台。

**核心功能**：
- 📰 新闻热点：词频聚合，实时推送
- 📈 价格信号：异动检测，触发推送
- 📊 K 线展示：双模式（TradingView 专业版 + Lightweight Charts 信号版）

## 核心页面设计

**一页整合：K 线 + 信号 + 新闻，时间轴对齐**

```
┌─────────────────────────────────────────────┬──────────────┐
│                                             │              │
│          TradingView（专业版）               │    新闻流     │
│          用户自由看盘、画线、加指标           │              │
│                                             │  10:00 ETF.. │
│                                             │  10:15 特朗普 │
├─────────────────────────────────────────────┤  10:30 SEC.. │
│                                             │              │
│      Lightweight Charts（信号版）            │    ▲         │
│      ▲买入    ▼卖出    ●新闻标记             │    时间轴    │
│      10:00   10:30    10:15                 │    对齐      │
│                                             │              │
└─────────────────────────────────────────────┴──────────────┘
```

**亮点**：
- 上方 TradingView：数据验证 + 专业分析
- 下方 Lightweight：信号标记 + 新闻标记
- 右侧新闻流：时间轴对齐，点击跳转
- 新闻也能打到 K 线图上：`●` 标记重大新闻时间点

**目标**：开源，本地自托管。

---

## 核心架构

```
┌─────────────────────────────────────────────────────────┐
│                        前端                              │
│                                                         │
│   新闻热点模块    信号推送模块    K线图表模块                        │
│   (GraphQL)      (Subscription)  (TradingView + Lightweight Charts) │
└─────────────────────┬─────────────────────────────────-─┘
                      │ GraphQL
                      ▼
┌─────────────────────────────────────────────────────────┐
│                  Hasura（自动 API）                      │
└─────────────────────┬───────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────┐
│              PostgreSQL + TimescaleDB                    │
│                                                         │
│   prices → indicators → signals   │   news → word_freq  │
│         (信号系统)                 │    (新闻热点)        │
│                                                         │
│   klines (1分钟K线，Lightweight Charts 展示 + 标记)       │
└─────────────────────┬───────────────────────────────────┘
                      ▲
                      │ 增删改查
┌─────────────────────┴───────────────────────────────────┐
│                  Python 程序                             │
│                                                         │
│   价格爬虫 → 指标计算 → 信号检测   │   新闻爬虫 → 词频统计 │
│   K线爬虫 (1分钟)                 │                      │
└─────────────────────────────────────────────────────────┘
```

**核心理念**：数据库即 API，维护好表就行。

---

## 关键设计决策

| 决策点 | 选择 | 理由 |
|--------|------|------|
| API 层 | Hasura (GraphQL) | 自动生成，不用手写接口 |
| 数据库 | PostgreSQL + TimescaleDB | 时序数据优化，Hasura 原生支持 |
| 爬虫调度 | APScheduler (BackgroundScheduler) | 轻量，每任务独立线程，不阻塞 |
| 页面爬虫 | Playwright | 绕过 API 限制，万物可爬 |
| 实时推送 | Hasura Subscriptions | 无需 Redis |
| K 线展示 | TradingView + Lightweight Charts | 双模式：专业看盘 + 信号标记 |
| 部署方式 | Docker Compose | 跨平台（Windows/Mac/Linux） |

---

## 数据源策略

| 数据类型 | 采集方式 | 用途 |
|---------|---------|------|
| 实时价格 | Ticker API（每 10 秒） | 计算指标、生成信号 |
| 1 分钟 K 线 | 交易所 API（每分钟） | Lightweight Charts 展示 + 标记 |
| 新闻 | Playwright 页面爬虫 | 热点聚合 |

---

## 信号系统

### 数据流

```
prices 表 → 计算 → indicators 表 → 判断 → signals 表
(原始数据)         (指标)              (信号)
```

### 信号触发示例

```python
# 每分钟计算指标
def calculate_indicators():
    prices = read_recent_prices(minutes=5)
    indicators = compute_change_pct(prices)
    write_to_db(indicators, 'indicators')

# 每分钟检测信号
def detect_signals():
    indicators = read_indicators()

    for row in indicators:
        if row.change_5min > 5:  # 5 分钟涨幅超 5%
            write_signal(row, 'pump_5min')
        if row.change_5min < -5:  # 5 分钟跌幅超 5%
            write_signal(row, 'dump_5min')
```

### 前端订阅信号

```graphql
subscription {
  signals(order_by: {time: desc}, limit: 10) {
    symbol
    signal_type
    change_pct
    time
  }
}
```

**用户体验**：不用盯盘，异动自动推送，点进去看 TradingView K 线。

---

## 新闻热点系统

### 交错调度

10 个新闻源，每个源 100 分钟轮一次，错开 10 分钟启动：

```
00:00 coindesk → 00:10 cointelegraph → 00:20 decrypt → ...
```

**效果**：每 10 分钟都有新内容，系统一直"活着"。

### 词频聚合

| 时间窗口 | 用途 |
|---------|------|
| 5 分钟 | 即时热点 |
| 1 小时 | 短期热点 |
| 24 小时 | 当日热点 |

最高词频 = 一组相关新闻 → 推送最新那条。

### 展示效果

```
🔴 5分钟热点：特朗普 (3条)
   └─ [最新] Trump announces new crypto policy...

🟠 1小时热点：ETF (8条)
   └─ [最新] Bitcoin ETF sees record inflows...

🟡 24小时热点：Solana (15条)
   └─ [最新] Solana hits new ATH...
```

---

## 数据存储策略

### 原始数据：可配置保留期（默认 7-30 天）

```sql
-- 保留期可配置，根据存储空间调整
SELECT add_retention_policy('prices', INTERVAL '${RETENTION_DAYS} days');
SELECT add_retention_policy('klines', INTERVAL '${RETENTION_DAYS} days');
SELECT add_retention_policy('news', INTERVAL '${RETENTION_DAYS} days');
```

**K 线存储量**：1 分钟级，单币种 30 天 ≈ 43200 根，100 个币 ≈ 4.3M 行，仍然很小。

### 计算结果：覆盖模式

| 表 | 策略 |
|---|------|
| indicators | 覆盖 |
| signals | 可配置保留期 |
| news_word_freq | 覆盖 |

**本地存储可控**：数据压缩后几 GB，单机够用。

---

## 待定事项

- 前端框架选型
- 新闻源列表（10 个）
- 信号阈值配置

---

*创建时间：2026-01-21*
